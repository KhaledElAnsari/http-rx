import { squash } from 'butternut';

function butternut$1(options) {
	return {
		name: 'butternut',

		transformBundle: function(code) {
			return squash(code, options);
		}
	};
}

export default butternut$1;
